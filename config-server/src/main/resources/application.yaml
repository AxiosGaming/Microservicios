server:
  port: 8088
spring:
  cloud:
    config:
      server:
        git:
          default-label: config-server
          uri: https://github.com/AxiosGaming/Microservicios
          search-paths: config-data
  application:
    name: config-service
          
          